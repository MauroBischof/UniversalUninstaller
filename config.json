{
    "__Timeout": "The following setting controls the timout period for all invoke commands in seconds.",
    "Timeout": 120,
    "__DebugMode": "The following setting controls the debugmode, for testing/debugging you can define a default host.",
    "DebugMode": true,
    "TargetComputer": "localhost",
    "__Delimiter": "The following setting contros the csv delimiter",
    "CSVDelimiter": ","
}
